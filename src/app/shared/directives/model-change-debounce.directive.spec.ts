import { ModelChangeDebounceDirective } from './model-change-debounce.directive';

describe('ModelChangeDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelChangeDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
